---
// src/components/heros/Hero01.astro
---

<section
  class="min-h-screen flex items-center justify-center p-4 sm:p-6 relative"
>
  <!-- Overlay sutil para mejorar contraste -->
  <div class="absolute inset-0 bg-black/30 backdrop-blur-[1px]"></div>

  <div class="text-center w-full max-w-4xl mx-auto px-4 sm:px-6 relative z-10">
    <span
      class="text-amber-400 font-mono tracking-widest text-xs sm:text-sm mb-3 sm:mb-4 block drop-shadow-lg"
    >
      LA LEYENDA VUELVE
    </span>

    <h1
      class="title text-4xl xs:text-5xl sm:text-6xl md:text-7xl lg:text-8xl font-bold mb-4 sm:mb-6 leading-tight"
    >
      <span class="block text-white drop-shadow-md">MERCEDES-AMG</span>
      <span class="text-white drop-shadow-lg">C 63</span>
    </h1>

    <div
      class="h-[1px] sm:h-[2px] w-16 sm:w-24 bg-gradient-to-r from-amber-400 to-yellow-300 mx-auto my-6 sm:my-8"
    >
    </div>

    <p
      class="text-base sm:text-xl md:text-2xl text-white/90 mb-8 sm:mb-12 max-w-2xl mx-auto leading-relaxed drop-shadow-[0_2px_4px_rgba(0,0,0,0.8)] px-2"
    >
      La combinación definitiva de rendimiento y lujo. Potencia pura, diseño
      atemporal.
    </p>

    <div
      class="flex flex-col sm:flex-row justify-center items-center gap-3 sm:gap-4 md:gap-6"
    >
      <a
        href="#especificaciones"
        class="w-full sm:w-auto inline-flex justify-center items-center px-4 sm:px-6 md:px-8 py-2 sm:py-2.5 md:py-3 border-2 border-white/30 text-white text-sm sm:text-base font-medium
                 hover:border-amber-400 hover:text-amber-300 transition-all duration-300
                 backdrop-blur-sm bg-white/5 hover:bg-white/10"
      >
        DESCUBRE MÁS
      </a>
      <a
        href="#configurador"
        class="w-full sm:w-auto inline-flex justify-center items-center px-4 sm:px-6 md:px-8 py-2 sm:py-2.5 md:py-3 bg-amber-500 text-black text-sm sm:text-base font-medium
                 hover:bg-amber-400 transition-all duration-300 border-2 border-transparent
                 hover:shadow-lg hover:shadow-amber-500/30"
      >
        CONFIGURAR
      </a>
    </div>
  </div>
</section>

<style>
  h1 {
    text-shadow: 0 2px 10px rgba(0, 0, 0, 0.7);
  }

  @media (max-width: 480px) {
    h1 {
      font-size: 2.5rem;
    }
  }

  @media (min-width: 481px) and (max-width: 767px) {
    h1 {
      font-size: 3rem;
    }
  }

  @media (min-width: 768px) and (max-width: 1023px) {
    h1 {
      font-size: 4rem;
    }
  }

  .title {
    animation: translate 1.5s ease-in-out both;
  }

  @keyframes translate {
    0% {
      transform: translateY(-200px);
    }
    100% {
      transform: translateY(0);
    }
  }
</style>
